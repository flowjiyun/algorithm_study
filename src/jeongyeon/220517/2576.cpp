#include <stdio.h>
/*
int main() {
	int ans = 100;
	int sum = 0;
	int a;
	for (int i = 0; i < 7; i++) {
		scanf("%d", &a);
		if (a % 2 == 1) {
			if (ans > a)
				ans = a;
			sum += a;
		}
	}
	if (sum == 0)
		printf("-1");
	else
		printf("%d\n%d", sum, ans);
}
*/