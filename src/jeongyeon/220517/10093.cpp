#include <stdio.h>
/*
int main() {
	long long a, b;
	int min;

	scanf("%lld %lld", &a, &b);
	if (a > b)
		min = a - b - 1;
	else if (b > a)
		min = b - a - 1;
	else
		min = 0;
	printf("%d\n", min);
	for (long long i = 1; i < min+1; i++) {
		if (a > b)
			printf("%lld ", b + i);
		else
			printf("%lld ",a + i);
	}
}
*/