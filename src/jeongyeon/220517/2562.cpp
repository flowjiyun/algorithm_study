#include <stdio.h>
/*
int main() {
	int arr[9], max = 0;
	for (int i = 0; i < 9; i++) {
		scanf("%d", &arr[i]);
	}
	for (int j = 0; j < 9; j++) {
		if (arr[j] > max)
			max = arr[j];
	}
	printf("%d\n", max);
	for (int k = 0; k < 9; k++) {
		if (arr[k] == max)
			printf("%d", k + 1);
	}
	return 0;
}
*/