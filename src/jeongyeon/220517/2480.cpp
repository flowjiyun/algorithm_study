#include <stdio.h>
/*
int main() {
	int x, y, z;
	scanf("%d %d %d", &x, &y, &z);
	if (x == y && y == z)
		printf("%d", 10000 + 1000*x);
	else if (x == y && !(y == z))
		printf("%d", 1000 + 100 * x);
	else if (x == z && !(y == z))
		printf("%d", 1000 + 100 * x);
	else if (y == z && !(x == y))
		printf("%d", 1000 + 100 * y);
	else {
		if (x > y && x > z)
			printf("%d", 100 * x);
		else if (y > z && y > x)
			printf("%d", 100 * y);
		else
			printf("%d", 100 * z);
	}
}
*/