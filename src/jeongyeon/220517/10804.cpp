#include <stdio.h>
/*
int main() {
	int arr[20], a, b, k,temp;
	for (int i = 0; i < 20; i++) {
		arr[i] = i + 1;
	}
	for (int i = 0; i < 10; i++) {
		scanf("%d %d", &a, &b);
		k = 1;
		for (int j = a; j <= (a + b) / 2; j++) {
			temp = arr[j - 1];
			arr[j - 1] = arr[b - k];
			arr[b - k] = temp;
			k++;
		}
	}
	for (int p = 0; p < 20; p++) {
		printf("%d ", arr[p]);
	}
	return 0;
}
*/