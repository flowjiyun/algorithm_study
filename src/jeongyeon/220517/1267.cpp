#include <stdio.h>
/*
int main() {
	int n,arr[20];
	int x= 0, y = 0;
	scanf("%d", &n);
	for (int i = 0; i < n; i++) {
		scanf("%d", &arr[i]);
		x += arr[i] / 30 + 1;
		y += arr[i] / 60 + 1;
	}
	x *= 10;
	y *= 15;
	if (x == y)
		printf("Y M %d", x);
	else if (x > y)
		printf("M %d", y);
	else
		printf("Y %d", x);
}
*/