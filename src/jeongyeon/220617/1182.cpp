#include <iostream>
/*
using namespace std;
int arr[21];
int n, s, ans = 0;

void fun(int current, int total) {
	if (current == n) {
		if (total == s) ans++;
		return;
	}
	fun(current + 1, total);
	fun(current + 1, total + arr[current]);
}
int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> n >> s;
	for (int i = 0; i < n; i++) 
		cin >> arr[i];
	fun(0, 0);
	if (s == 0) ans--;
	cout << ans;
}
*/