#include <iostream>
#include <queue>
#include <string>
/*
using namespace std;
int main() {
	ios_base::sync_with_stdio(0);
	cin.tie(0); cout.tie(0);
	queue<int> q;
	int n;
	cin >> n;
	for (int i = 1; i <= n; i++) {
		q.push(i);
	}
	while (q.size() != 1) {
		q.pop();
		int n;
		n = q.front();
		q.push(n);
		q.pop();
	}
	cout << q.front() << endl;
}
*/