#include <iostream>
#include <stack>
#include <string>
/*
using namespace std;

int main() {
	stack<int> l;
	int n;
	cin >> n;
	while (n--) {
		string str;
		cin >> str;
		if (str == "push") {
			int x;
			cin >> x;
			l.push(x);
		}
		else if (str == "pop") {
			if (l.empty())
				cout << -1 << "\n";
			else {
				cout << l.top() << "\n";
				l.pop();
			}
		}
		else if (str == "size") {
			cout << l.size() << "\n";
		}
		else if (str == "top") {
			if (l.empty())
				cout << -1 << "\n";
			else
				cout << l.top() << "\n";
		}
		else {
			cout << l.empty() << "\n";
		}
	}
	return 0;
}
*/